import * as React from 'react';
import { ErrorBoundary } from '../../utils/ErrorBoundary';

const Footer = () => {
	return (
		<ErrorBoundary>
			Footer
		</ErrorBoundary>
	)
};

export default Footer;